# ~/.tmuxinator/gi.yml

name: gi
root: ~/src/getty/unisporkal

windows:
  - gettyservers:
      layout: tiled
      panes:
        - docker:
          - cd gi_proxy
          - gid
        - uni:
          - ls
        - dynamo:
          - gidynamo
        - dynamoadmin:
          - gidynamoadmin

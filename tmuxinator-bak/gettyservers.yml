# ~/.tmuxinator/gettyservers.yml

name: gettyservers
root: ~/src/getty/unisporkal

windows:
  - gettyservers:
      layout: main-vertical
      panes:
        - docker:
          - cd gi_proxy
          - gidocker
        - dynamo:
          - gidynamo
        - dynamoadmin:
          - gidynamoadmin

# ~/.tmuxinator/styles.yml

name: styles
root: <%= ENV['UNISPORKAL'] %>/gems/unisporkal_styles

windows:
  - styles:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
        - term:

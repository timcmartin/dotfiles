# ~/.tmuxinator/giadmin.yml

name: giadmin
root: <%= ENV['UNISPORKAL'] %>

windows:
  - main:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
        - term:
  - services:
      layout: even-horizontal
      panes:
        - proxy:
            - echo "💡 Run 'giproxy-a' here"
        - dynamo:
            - echo "💡 Run 'cd $HOME/src/getty/gi-local-dynamo && bin/start' here"

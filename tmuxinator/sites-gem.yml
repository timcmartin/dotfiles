# ~/.tmuxinator/sites-gem.yml

name: sites-gem
root: <%= ENV['UNISPORKAL'] %>/gems/sites

windows:
  - sites-gem:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
        - term:

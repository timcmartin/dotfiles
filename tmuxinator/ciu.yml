# ~/.tmuxinator/ciu.yml
name: ciu
root: ~/src/getty/unisporkal/curated-image-uploader

windows:
  - ciu:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
        - term:
  - services:
      layout: even-horizontal
      panes:
        - rails:
            - echo "💡 Run 'export AWS_PROFILE=local && gir' here"
        - guard:
            - echo "💡 Run 'be guard -c' here"

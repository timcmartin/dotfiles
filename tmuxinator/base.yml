# ~/.tmuxinator/base.yml

name: base
root: <%= ENV['UNISPORKAL'] %>

windows:
  - cns:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
  - term:
      layout: even-horizontal
      panes:
        - term:

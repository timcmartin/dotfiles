# ~/.tmuxinator/sites-pkg.yml

name: sites-pkg
root: <%= ENV['UNISPORKAL'] %>/packages/sites

windows:
  - sites-package:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
        - term:
            - echo "💡 Run 'yarn build' here"

# ~/.tmuxinator/recipes.yml

name: recipes
root: ~/src/personal/recipes

windows:
  - recipes:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
        - term:
  - gitbook:
      layout: even-horizontal
      panes:
        - server:
            - echo "💡 Run 'gitbook serve .' here"
        - git:

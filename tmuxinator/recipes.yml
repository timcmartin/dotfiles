# ~/.tmuxinator/recipes.yml

name: recipes
root: ~/src/personal/recipes

windows:
  - recipes:
      layout: main-vertical
      panes:
        - nvim:
            - nvim
        - term:
  - gitbook:
      layout: main-vertical
      panes:
        - server:
            - echo "💡 Run 'gitbook serve .' here"
        - git:

# ~/.tmuxinator/engine.yml

name: engine
root: <%= ENV['UNISPORKAL'] %>/gems/unisporkal_engine

windows:
  - engine:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
        - term:

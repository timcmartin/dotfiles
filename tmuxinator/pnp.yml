# ~/.tmuxinator/plans-and-pricing.yml

name: plans-and-pricing
root: <%= ENV['UNISPORKAL'] %>/federated-components/plans-and-pricing

windows:
  - pnp:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
        - term:
  - pnp-server:
      layout: even-horizontal
      panes:
        - term:
        - pnpm:
            - echo "💡 Run 'pnpm dev' here"

# ~/.tmuxinator/search.yml

name: search
root: <%= ENV['UNISPORKAL'] %>/search

windows:
  - search:
      layout: even-horizontal
      panes:
        - nvim:
            - nvim
        - term:
  - servers:
      layout: even-horizontal
      panes:
        - rails:
            - echo "💡 Run 'gir' here (rails server)"
        - assets:
            - echo "💡 Run 'giw' here (webpack dev server)"
